interface Props {
  name: string;
  region: string;
  coatOfArms: string;
  words: string;
  titles: string;
  seats: string;
  overlord: string;
  diedOut: string;
  codeBranches: string;
}

const HouseDetails = ({
  name,
  region,
  coatOfArms,
  words,
  titles,
  seats,
  overlord,
  diedOut,
  codeBranches,
}: Props) => {
  return null;
};

export default HouseDetails;
